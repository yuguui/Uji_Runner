<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.uji.al341520.breakthewall">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

    <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".testFramework.TestFramework"/>
        <activity android:name=".testParallax.TestParallax"/>
        <activity android:name=".testCharacter.TestCharacter"/>
        <activity android:name=".testObstacles.TestObstacles"/>
        <activity android:name=".testLevelsHUD.TestLevelsHud"/>
        <activity android:name=".UjiRunner.UjiRunner"/>



    </application>

</manifest>